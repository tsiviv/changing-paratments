{
  "name": "fullstack-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd react-project && npm install && npm run build",
    "start": "cd server && npm install && npm start"
  }
}
